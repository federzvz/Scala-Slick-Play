# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

GET     /api/reservas/:id          co.com.reservas.restaurantes.infraestructura.controladores.ControladorReserva.obtenerReserva(id: String)
POST     /api/reservas            co.com.reservas.restaurantes.infraestructura.controladores.ControladorReserva.crearReserva
PUT     /api/reservas           co.com.reservas.restaurantes.infraestructura.controladores.ControladorReserva.actualizarReserva
DELETE /api/reservas/:id          co.com.reservas.restaurantes.infraestructura.controladores.ControladorReserva.eliminarReserva(id: String)